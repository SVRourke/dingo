require "spec_helper"

describe ImagePolicy do
    subject {ImagePolicy.new(user, article)}

    
    # context "not signed in" do
    #     let(:user) {nil}
    #     it {should_not permit(:show)}
    # end
end