<table>
    <tr>
        <td>image:</td>
        <td>caption:</td>
    </tr>
<% @images.each do |image| %>
    <tr>
        <td><%= image.id %></td>
        <td><%= image.caption %></td>
        <td><%= link_to 'view', image_path(image) %></td>
    </tr>
<% end %>
</table>